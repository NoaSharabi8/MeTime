<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".Activities.ServiceSelectionActivity"
    android:background="@color/background">


    <!-- Title of Service Selection -->
    <TextView
        android:id="@+id/service_selection_TV_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="7dp"
        android:text="@string/choose_your_treatment"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/pink"/>

    <!-- Expandable List of Treatments -->
    <FrameLayout
        android:id="@+id/service_selection_LISTVIEW_service_list"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:padding="8dp"
        android:layout_marginTop="25dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/frame_outline"
        android:layout_below="@id/service_selection_TV_title"
        android:layout_above="@+id/service_selection_BTN_next"
        />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/service_selection_BTN_next"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/next"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="@color/pink"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="18sp"/>



</RelativeLayout>
